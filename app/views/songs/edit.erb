<h1>Edit Song</h1>

<div class="song-header">
  <h2><%= @song.name %></h2>
  <p><%= @artist_names %></p>
  <p><%= @song.album.title %></p>
</div>

<%= render partial: "new_section_form" %>

<%= render partial: "add_section_button" %>

<div class="song-edit">
  <%= render partial: 'show_song', locals: { song: @song } %>
</div>
