<div class="edit-measure">
  <input type="hidden" name="measure_id" value="<%= measure.id %>">
  <div class="row">
    <!-- refactor to have to translate cell into html for the cell -->
    <% measure.ordered_cells.each do |cell| %>
      <div class="col select <%= cell.html('stressed') %> <%= cell.html('end_rhyme') %> text-center" name="<%= cell.id %>" style="width:<%= cell.note_duration/9.6 %>%" data-rhyme="<%= cell.rhyme %>">
        <%= cell.content %>
      </div>
    <% end %>
  </div>
</div>

