<div class="container">
  <h1>Rap Analysis Webapp Homepage</h1>

  <ul>
    <% @songs.each do |song| %>
    <li>
      <a href="songs/<%= song.id %>"><%= song.name %> :: <%= song.artists.first.name %> :: <%= song.album.title %></a>
    </li>
    <% end %>
  </ul>

</div>
